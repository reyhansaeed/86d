{
	"name": "@86d-app/internals-generators",
	"version": "0.0.1",
	"private": true,
	"license": "MIT",
	"type": "module",
	"scripts": {
		"check": "biome check .",
		"check:fix": "biome check --write --unsafe .",
		"clean": "git clean -xdf .cache .turbo dist node_modules",
		"test": "vitest run"
	},
	"dependencies": {
		"@86d-app/registry": "workspace:*",
		"env": "workspace:*"
	},
	"devDependencies": {
		"@biomejs/biome": "catalog:",
		"tsx": "catalog:",
		"vitest": "catalog:vite"
	}
}
